#include<stdio.h>
int main()
{
    long long int cases, n;
    scanf("%lld",&cases);
    while(cases--)
    {
        scanf("%lld", &n);
        if(n%2 == 0)
            printf("YES\n");
        else
            printf("NO\n");
    }
    return 0;
}
